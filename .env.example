# Generated by `npx convex dev` — copy values into .env.local
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
# Optional: site URL for auth callbacks (local)
# CONVEX_SITE_URL=http://localhost:3000
